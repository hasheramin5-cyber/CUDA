# GPU Vector Multiplication

A practical CUDA project demonstrating element-wise vector multiplication using GPU parallel processing.

## What This Project Does

The program creates two vectors containing 1,000,000 elements and multiplies corresponding elements using a CUDA kernel.

The operation performed is:

```text
C[i] = A[i] × B[i]

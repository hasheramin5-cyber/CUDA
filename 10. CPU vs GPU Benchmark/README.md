# CPU vs GPU Benchmark

A practical CUDA project comparing CPU and GPU execution time for a large vector addition workload.

## What This Project Does

The program performs the same vector addition operation on both the CPU and NVIDIA GPU.

The operation performed is:

```text
C[i] = A[i] + B[i]
